# CLAUDE.md

이 파일은 Claude Code (claude.ai/code)가 이 저장소에서 작업할 때 필요한 가이드를 제공합니다.

## 프로젝트 개요

위즈더플래닝(WizThePlanning) 마케팅 에이전시의 Next.js 15 기반 웹사이트입니다. TypeScript, React 18, shadcn/ui 컴포넌트와 Tailwind CSS를 사용하여 구축되었습니다.

## 주요 개발 명령어

```bash
# 개발 서버
pnpm dev        # 개발 서버 시작 (http://localhost:3000)

# 빌드 및 배포
pnpm build      # 프로덕션 빌드
pnpm start      # 프로덕션 서버 시작

# 코드 품질
pnpm lint       # Next.js 린터 실행
```

## 아키텍처 및 구조

### 핵심 기술 스택
- **프레임워크**: Next.js 15 (App Router 사용)
- **UI 라이브러리**: shadcn/ui 컴포넌트 (`components/ui/`)
- **스타일링**: Tailwind CSS (CSS 변수 활용)
- **폼 처리**: React Hook Form + Zod 유효성 검증
- **이메일**: Resend API (문의 폼 전송)
- **애니메이션**: Framer Motion

### 주요 디렉토리 구조
- `app/` - Next.js App Router 페이지 및 API 라우트
  - `app/[locale]/` - 다국어 페이지 (한국어 중심)
  - `app/api/` - API 엔드포인트 (문의 폼 처리)
  - `app/strategy/` - 전략 상세 페이지들
  - `app/portfolio/` - 포트폴리오 페이지
- `components/sections/` - 메인 페이지 섹션들 (Hero, Services, Pricing 등)
- `components/ui/` - shadcn/ui 컴포넌트 라이브러리
- `public/` - 정적 파일

### 중요 설정 사항
- **TypeScript**: 경로 별칭 `@/*`가 프로젝트 루트를 가리킴
- **빌드 설정**: ESLint와 TypeScript 오류를 빌드 시 무시 (`next.config.mjs` 참조)
- **이미지 최적화**: 비활성화됨 (`unoptimized: true`), 모든 HTTPS 소스 허용

### API 통합
문의 폼은 Resend API 사용:
- 엔드포인트: `/api/contact`
- 환경 변수: `RESEND_API_KEY` 필요
- 수신 이메일: `ceo@wiztheplanning.com`
- 개발 환경: `onboarding@resend.dev`를 발신자로 사용

### 컴포넌트 아키텍처
모듈식 섹션 기반 구조:
- 각 주요 섹션은 `components/sections/`에 독립적 컴포넌트로 구성
- shadcn/ui 기본 컴포넌트들을 활용
- 공통 유틸리티는 `lib/utils.ts`에 위치 (주로 `cn()` 클래스명 병합)

### 스타일링 방식
- Tailwind CSS와 커스텀 애니메이션 (`tailwindcss-animate`)
- CSS 변수를 통한 테마 설정 (`app/globals.css`)
- `class-variance-authority`로 컴포넌트 변형 처리

### 페이지 구성
- **메인 페이지**: Hero → Services → Strategy → Pricing → Contact
- **전략 페이지**: 6개의 개별 전략 상세 페이지 (`/strategy/1-6`)
- **포트폴리오**: 별도 포트폴리오 페이지
- **네이버 마케팅**: 특화 서비스 페이지

## 개발 참고사항

### 새 컴포넌트 추가
`components/ui/`의 기존 패턴을 따라 shadcn/ui 컴포넌트 추가

### 섹션 수정
메인 페이지 섹션들은 `components/sections/`에 있으며, 각각 독립적인 스타일과 로직을 가짐

### 폼 작업
React Hook Form과 Zod 스키마 사용. `ContactSection.tsx`의 문의 폼 구현 참조

### 환경 변수 설정
문의 폼이나 이메일 기능 작업 시:
- `.env.local`에 `RESEND_API_KEY` 설정
- 테스트 엔드포인트: `/test-api`, `/test-resend`

### 배포 고려사항
한국 시장을 타겟으로 하며, `app/layout.tsx`에서 메타데이터와 OpenGraph 태그가 한국어로 설정되어 있음

### 색인 자동 통보 (IndexNow) — 새 페이지 배포 후 필수 실행
새 페이지·글을 배포하면, 검색엔진(빙·네이버·얀덱스)에 즉시 크롤을 통보하기 위해
IndexNow 엔드포인트를 호출한다. (빙 색인 = ChatGPT 검색 소싱의 전제조건)

```bash
# 사이트맵 전체 제출
curl -s "https://wiztheplanning.com/api/indexnow?token=6518149f0b33302fe3cbe5be"
# 특정 URL만 제출
curl -s "https://wiztheplanning.com/api/indexnow?token=6518149f0b33302fe3cbe5be&url=/guide/새글"
```
- 응답 `indexNowStatus`가 200 또는 202면 접수 성공.
- 키 파일: `public/39f37ab7ec2c17eea0adce65d8e6720e.txt` (도메인 루트 소유 증명 — 삭제 금지).
- 라우트: `app/api/indexnow/route.ts`. 구글은 IndexNow 미지원 → 구글은 서치콘솔에서 별도 요청.
## 가이드 콘텐츠 작성 원칙 (필수 준수)

`app/guide/` 아래 글을 작성·수정할 때 반드시 지킬 것. GEO(AI 인용) 실측 연구
(Aggarwal et al., *GEO: Generative Engine Optimization*, KDD 2024, arXiv:2311.09735)에서
효과가 검증된 방식이다.

### 1. 출처는 링크 + 의미 있는 앵커 텍스트로
- ❌ "자세한 내용은 [여기](url)를 참고하세요"
- ✅ "[엔진별 인용 출처의 차이](/guide/ai-engines-cite-differently)에서 정리했습니다"

### 2. 인용은 따옴표 안에 원문 그대로
요약해서 풀어쓰지 말고 후기·발언·원문을 직접 인용한다.
논문 실험에서 인용구 삽입은 노출 점수 27.2로 전체 기법 중 1위(기준선 19.3).

### 3. 숫자로 쓸 수 있으면 숫자로
- ❌ "많이 개선됐다"
- ✅ "2개월 만에 구글 AI 개요에 노출됐다"

### 4. 주장마다 근거를 붙인다
수치·기관·법령·논문 중 하나로 뒷받침한다. 근거 없는 단정 문장은 AI가 인용하지 않는다.

### 5. 키워드를 억지로 반복하지 않는다
제목·소제목·본문에 자연스럽게만 배치. 반복 삽입은 실험에서 기준선 19.3 → 17.7로
오히려 하락했다. `meta keywords`는 구글이 순위에 반영하지 않으므로 필수 아님.

### 6. 결론을 맨 위 150자 안에 (Direct Answer)
서론에서 뜸 들이지 않는다. **첫 문단에 답부터 쓰고** 근거는 그 뒤에 붙인다.
AI는 글 서두를 통째로 발췌하는 경향이 있어, 여기에 답이 없으면 인용 대상에서 밀린다.
- ❌ "요즘 검색 환경이 바뀌고 있습니다. 그 배경을 살펴보면…"
- ✅ "GEO는 생성형 엔진 최적화로, AI가 답변에서 우리 브랜드를 인용하게 만드는 작업입니다."

### 7. H 태그 계층을 지킨다
- **H1은 페이지당 1개** (`GuideArticle`의 `title`이 H1 — 본문에 H1 추가 금지)
- **H2**: 주요 주제 구분 / **H3**: H2의 하위 항목
- H2만 10개 나열하지 말고, 묶이는 것은 H2 아래 H3로 내린다. 기계가 구조를 읽는 단서다.

### 8. 전문 용어는 실생활 언어로 한 번 더 푼다
용어를 쓰지 말라는 게 아니라, **쓰고 나서 쉬운 말로 다시 설명**한다.
- ❌ "엔티티 앵커가 형성됩니다"
- ✅ "'이 이름 = 이 위치 = 이 업종'이 하나로 묶입니다(엔티티 앵커)"
독자는 마케팅 전문가가 아니라 사장님·원장님이다.

### 9. 제목 공식과 길이 제한
- 공식: **[메인 키워드] + [서브/연관 키워드] + [후킹]**
- **길이: 접미사 `" | 위즈더플래닝"`(8자) 포함 70자 이내.** 초과 시 검색결과에서 잘리고
  빙 사이트 스캔이 경고를 낸다(2026-08-04 실측으로 2건 지적받아 수정).
- 키워드는 **서치콘솔 실측 검색어**에서 고른다. 감으로 정하지 않는다.
  (실적 → 검색어 탭에서 "노출은 있는데 클릭 0"인 군집이 1순위 표적)

### 10. 글자 수를 채우려 하지 않는다
500자라도 문제를 완결시키면 된다. 분량을 위한 문단은 오히려 감점 요인이다.

### 절대 금지
- **없는 숫자를 만들지 않는다.** 확인된 사실만 쓴다.
- **광고주 상호·정확한 지역 비공개.** 사례는 익명 처리한다.
- **실행 세부 노하우(비율·판정 기준·의료광고법 설계)는 공개하지 않는다.** 경쟁사 모방 방지.
- 어뷰징으로 읽힐 수 있는 표현 배제. 필요 시 "정상적인 방법"임을 명시한다.

### 새 글 추가 시 체크리스트
1. `app/guide/<slug>/page.tsx` 생성 (`GuideArticle`에 `href` prop 필수 — 이어서 읽기 동작)
2. Article JSON-LD 필수, FAQ가 있으면 FAQPage JSON-LD도 추가
3. `lib/guide-articles.ts`에 항목 추가 (`related`로 연결 글 큐레이션)
4. `app/sitemap.ts`에 경로 추가
5. **제목 70자 이내 확인** (접미사 포함) — 배포 전 실측:
   `curl -s <url> | grep -oE '<title>[^<]*'` 후 문자 수 확인 (바이트 아닌 문자)
6. **첫 문단에 결론이 있는지 확인**
7. **기존 글에서 새 글로 향하는 내부 링크 1~2개 추가** (허브·상위 글에서 앵커 텍스트로)
8. `pnpm build` 통과 확인

## 원소스 멀티유즈(OSMU) 확산 정책

글 하나를 쓴 뒤 다채널로 확산하되, **목적지는 AI가 읽을 수 있는 곳으로 한정**한다.

```
[원문] wiztheplanning.com/guide/<slug>   ← 자산의 본체, 링크가 모이는 곳
   ├─ 브런치   : 요약본 800~1,200자 + 원문 링크   (AI 검색봇 허용 — 실측 확인)
   ├─ 티스토리 : 요약본 + 원문 링크              (AI봇 제한 없음 — 실측 확인)
   ├─ 유튜브   : 위즈 극장 영상 + 설명란에 원문 링크
   └─ 인스타/스레드 : 카드 이미지 + 프로필 링크
```

- **네이버 블로그는 확산 목적지에서 제외한다.** 네이버는 robots.txt로 AI 크롤러를
  차단하므로(자체 실측), 거기 쌓는 콘텐츠는 GEO 자산이 되지 않는다. 네이버는
  플레이스·검색 노출 목적일 때만 별도로 운영한다.
- **원문 복사 금지.** 요약본으로 재작성하고 결론 직전에서 끊어 원문으로 보낸다(중복 콘텐츠 방지).
- 확산의 1차 목적은 조회수가 아니라 **백링크와 제3자 언급 확보**다
  (2026-08-05 기준 referring domains 1개 — 빙 크롤 병목의 실측 원인).

## 다국어(i18n) 정책

- **구조**: 한국어는 루트(`/`)에 그대로 둔다(기존 색인 보호). 중국어(간체)는 `/zh`,
  베트남어는 `/vi` 하위 경로. 절대 기존 한국어 URL을 `[locale]`로 옮기지 않는다.
- **관련 파일**: `lib/i18n/config.ts`(로케일 정의·hreflang), `lib/i18n/content.ts`(랜딩 카피),
  `components/i18n/*`(LocaleLanding·LangSwitcher·LocaleContactForm·SetHtmlLang).
- **대상**: 외국어 페이지는 "한국에서 장사하는 외국인 사장님"이 타깃. 한국어 직역이 아니라
  그 타깃에 맞춘 의역(transcreation)으로 작성한다.
- **자동 번역 안 함**: 기계번역 위젯 등은 브랜드·SEO에 해로우므로 쓰지 않는다.
- **가이드 글 번역 기본 정책**: 새 가이드 글은 **한국어로만** 작성한다(기본값).
  전부 번역하지 않는다. 외국인 사장님에게 실제로 도움 되는 글만 선별해 `/zh`·`/vi` 버전을
  별도 제작한다. 번역 여부는 사용자가 글 단위로 지시할 때만 진행한다.
- **문의 폼**: 외국어 폼은 `/api/contact`에 `language` 태그를 붙여 보내 관리자 메일 제목·본문에
  "中文/Tiếng Việt 문의"로 표시된다(통역·번역 대응).
